# --- CORE & AGENTS (Era Giugno 2024) ---
livekit-agents==0.12.1
# È importante bloccare anche il core SDK per evitare discrepanze interne
livekit==0.12.2

# --- PLUGINS (Versioni compatibili con 0.12.x) ---
# La versione 0.6.0 è quella stabile per l'era Pipeline
livekit-plugins-openai==0.6.0
livekit-plugins-deepgram==0.6.0
livekit-plugins-elevenlabs==0.6.0
livekit-plugins-silero==0.6.0

# --- UTILITY ---
python-dotenv
aiohttp
